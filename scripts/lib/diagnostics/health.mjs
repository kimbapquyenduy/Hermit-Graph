export function runtimeHealth(store,recorder,scope={}){const summary=store.summary(scope),total=summary.successes+summary.failures;return {section:'Runtime Health',...summary,failureRate:total?summary.failures/total:0,score:total?Math.round(100*summary.successes/total):100,...recorder?.health(),limitations:['Operational metadata only; no prompt, argument, result, message or stack capture.','Pending in-memory evidence can be lost on abrupt exit.']};}
